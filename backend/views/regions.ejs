<!DOCTYPE html>
<html lang="fr">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Géographie | Régions</title>
        <!--Bootstrap CSS-->
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
        <!-- Ajouter Bootstrap JS (selon les fonctionnalités utilisées) -->
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    </head>
    <body>
        <%- include('./navbar.ejs') %>
        <div class="container mt-5">
            <h1 class="mb-4">Données de l'API Métadonnées | Géographie | Régions</h1>
            <!--Tableau de données-->
            <table class="table table-striped table-bordered">
                <thead class="thead-dark">
                    <th>Code</th>
                    <th>Uri</th>
                    <th>Type</th>
                    <th>Date de création</th>
                    <th>Intitulé (sans article)</th>
                    <th>Type d'article</th>
                    <th>Intitulé</th>
                </thead>
                <tbody>
                    <% data.forEach(entry => { %>
                        <tr>
                            <td><%= entry.code ? entry.code : 'N/A' %></td>
                            <td><%= entry.uri ? entry.uri : 'N/A' %></td>
                            <td><%= entry.type ? entry.type : 'N/A' %></td>
                            <td><%= entry.dateCreation ? entry.dateCreation : 'N/A' %></td>
                            <td><%= entry.intituleSansArticle ? entry.intituleSansArticle : 'N/A' %></td>
                            <td><%= entry.typeArticle ? entry.typeArticle : 'N/A' %></td>
                            <td><%= entry.intitule ? entry.intitule : 'N/A' %></td>
                        </tr>
                    <% }) %>
                </tbody>
            </table>
        </div>
       
    </body>
</html>